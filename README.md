# Projects

Create a folder and add all these files in it 
Make sure to open folder in vs code, in which files are saved

Note : open whole folder the files will run only when they are saved at same place/folder
